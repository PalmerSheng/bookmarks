# Supabase 配置
# 在 Cloudflare Pages 项目设置 > 环境变量 中配置这些变量

# Supabase Edge Function URL
VITE_SUPABASE_URL=https://husdiczqouillhvovodl.supabase.co/functions/v1/clever-action

# Supabase Anon Key - 替换为你的实际 anon key
# 可以在 Supabase 项目设置 > API 中找到
VITE_SUPABASE_TOKEN=your_supabase_anon_key_here

# 翻译功能配置
# Supabase 翻译 Edge Function URL
VITE_SUPABASE_TRANSLATE_URL=https://your-project.supabase.co/functions/v1/translate

# Supabase Anon Key (用于翻译功能)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Clerk 用户认证配置
# 在 Clerk Dashboard 中获取这些配置
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here

# 注意事项:
# 1. 环境变量名必须以 VITE_ 开头才能在客户端访问
# 2. 在 Cloudflare Pages 中设置环境变量后需要重新部署
# 3. 请勿在代码中硬编码敏感信息
# 4. Cloudflare AI 的 API Token 应该在 Supabase Edge Function 的环境变量中设置，不要在客户端暴露
# 5. Clerk Publishable Key 可以安全地在客户端使用 